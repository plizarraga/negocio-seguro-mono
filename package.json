{
  "devDependencies": {
    "turbo": "^1.10.13"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "export NODE_ENV=production && node apps/api/dist/main"
  },
  "workspaces": [
    "apps/*"
  ]
}